# 0.3.6 功能测试记录

本记录更新于 2026-09-12。仓库准备版本为 0.3.6，未创建版本标签或 GitHub Release。[0.3.5 的原始测试记录](./docs/testing-0.3.5.md)完整保留，以下结果均来自本轮验证。桌面重启后的复测、四种真实系统剪贴板格式，以及桌面双图片入队、识别和清理均已通过。

## 环境与方法

本机为 Windows，Node.js 24.19.0、Claude Code 2.1.261、Codex CLI 0.153.4。自动化测试使用模拟 Claude 进程、临时项目、隔离的插件状态和剪贴板夹具。真实调用使用 DeepSeek 官方 Anthropic 兼容接口与 `deepseek-flash[1m]`，只处理专用测试目录中的固定内容。经用户授权，原生剪贴板验证向系统剪贴板写入合成测试图片，再调用已安装的 0.3.6 捕获入口，保持其默认受限子进程环境和 15 秒超时。

完整检查共 136 项，全部通过，无失败或跳过。检查包含 JavaScript 语法、插件结构、版本一致性和 UTF-8 编码。插件技能提供的独立清单校验器也通过。代码经过分工审查和交叉审查，没有执行哈希检验。

```powershell
npm run check
```

GitHub CI 覆盖 Windows、Ubuntu、macOS 与 Node.js 18、20、22、24，共 12 个组合。非 Windows 环境跳过 Windows 清单入口和原生剪贴板助手的两项测试，其余照常运行。最终代码及夹具修复后的 [12 个组合均通过](https://github.com/yeyiwen2006/codex-claude-code-bridge/actions/runs/34683107791)，后续文档提交的运行结果见 [GitHub CI](https://github.com/yeyiwen2006/codex-claude-code-bridge/actions/workflows/ci.yml)。

首轮 CI 暴露了 macOS 目录别名导致注册位置误判的问题，注册脚本已改为比较真实目录，并补充目录别名回归。Windows 新增路径测试也改为统一长短文件名后比较。新增任务恢复夹具后来也因未保存规范路径而失败，现已与生产授权和图片入口保持一致，并在 Windows junction 别名目录中通过定向测试。剪贴板测试在云端曾超时，增加阶段日志后确认 PowerShell 已进入脚本，阻塞发生在夹具读取所触发的模块加载环节；后续修复直接读取夹具，并明确加载系统自带模块，避免自动搜索其他模块目录。生产捕获超时未调整。

## 本轮发现并修复的问题

| 功能 | 发现的问题 | 修复及验证 |
| --- | --- | --- |
| 审批通信 | MCP 等待用户决定时阻塞 `ping` 和取消消息 | 保持通信响应，串行处理审批；取消旧请求后可处理下一项审批 |
| 审批状态 | 迟到的请求或决定可能让已结束任务恢复运行 | 在状态锁内重新检查任务、审批和取消标志；拒绝终态请求 |
| 后台任务 | 启动失败遗留 `starting` 状态，启动前的取消仍可能调用 Claude | 处理同步与异步启动错误，清理未执行的输入；启动前检查取消 |
| 宿主停止与恢复 | 桌面停止按钮终止 worker 后，任务可能永远停留在运行或取消中 | 仅在确认登记的 worker 已退出后回收任务；状态、结果、再次运行和 SessionEnd 均可触发恢复；保留未确认输出并明确标记取消或失败 |
| 会话状态 | 128 字符的合法会话 ID 加上锁名前缀后超限 | 锁名上限纳入前缀，最长 ID 的读写和加锁正常 |
| MCP 协议 | 错误消息、无 ID 的请求、重复活动 ID 和非对象参数可进入错误处理路径 | 校验两条 MCP 通道的消息格式；拒绝重复请求和错误参数；未知工具返回协议错误 |
| 写入并发 | 图片父目录会通过 `--add-dir` 授予 Claude，却没有加入写锁 | 将图片父目录与工作目录、附加目录一并互斥 |
| 图片清理 | junction 或符号链接替换队列目录后，清理可能删除外部文件 | 以插件数据目录的真实路径检查图片根、会话、批次和文件父目录；正常数据目录别名仍可使用 |
| 中断后的图片状态 | 最后一张图片及其目录已删除，但 worker 尚未保存新状态 | 确认目录项缺失且父目录有效后，只移除本次选中的过期记录；保留其他队列记录，拒绝悬空链接和根目录重定向 |
| 剪贴板捕获 | 助手中途失败会残留文件，文件列表复制后才检查大小与数量 | 捕获按批次隔离并清理失败批次；复制前限制 20 张、单张 25 MiB、总量 100 MiB；PNG 流限制写入量 |
| 对话继承 | 与用户请求同条保存的宿主配置前导被传给 Claude | 去掉已识别的宿主前导，保留后面的用户请求 |
| 配置命令 | `constructor` 等无效重置键被报告为成功 | 使用自有属性检查，拒绝无效键并保留已有配置 |
| 模型服务配置 | 受限 MCP 路径丢失自定义模型及别名映射 | 清单与子进程环境同步透传五个明确的模型配置变量 |
| 安装与编码 | 无效市场名称仍被写入，BOM 检查会漏检 | 非法名称不改变文件；编码检查明确拒绝 BOM、无效 UTF-8 与替换字符 |

其他自动化覆盖包括全部公开配置项、App 与 CLI 命令语法、附件包装、命令去重、正常完成、超时、取消、SessionEnd 清理、允许与拒绝、`AskUserQuestion`、跨目录授权、会话恢复约束、图片顺序与去重、原生权限模式映射、结果恢复、文件操作报告、进程输出限制、注册与卸载时保留其他插件。

## 真实环境验证

| 功能 | 本轮结果 |
| --- | --- |
| Claude 健康检查 | 检测到 2.1.261，认证状态有效 |
| MCP 只读调用 | 实际读取固定文件，返回预期中文内容 |
| MCP `acceptEdits` | 实际创建指定文件，文件内容与预期一致 |
| 当前 App 的 MCP 入口 | 通过当前已连接插件实际授权目录、读取中文文件、写入并回读测试文件 |
| 当前 App 的图片输入 | 经 MCP 传入专用 PNG，DeepSeek 正确识别左侧红色正方形和右侧蓝色圆形 |
| `safe` 标准输入审批 | 实际 Write 请求暂停，`allow once` 后同一任务继续，文件内容正确 |
| `plugin-only` MCP 审批 | 只加载显式测试插件，实际 Write 请求经权限 MCP 暂停和恢复 |
| Claude 插件与 Skill | 发现并执行 `bridge-release:echo`，生成指定中文测试文件 |
| 原生会话续接 | 关闭桥接器对话继承后，仍通过同一 Claude 会话 ID 复述上一轮标记 |
| 原生会话分叉 | 产生新的 Claude 会话 ID，并正确继承此前标记 |
| 本地安装 | 使用 0.3.6 的独立缓存版本重新安装成功；公开源清单保留纯版本号 |
| 最终安装的恢复入口 | 使用最终安装代码的正常 Hook 状态入口，将此前桌面测试遗留的取消中任务恢复为 cancelled；没有直接编辑会话状态文件 |
| 实际 Codex CLI Hook | `claude help` 被拦截，CLI 报告宿主输入和输出 token 均为 0 |
| 持久 Codex 宿主 | 同一 app-server 会话连续 11 个回合完成授权、配置、自动写入、手动 Write 审批、批准恢复、取消和退出清理；无宿主模型调用项或 token 事件 |
| 最终代码的宿主停止与恢复 | 在新启动的隔离 app-server 中，真实 interrupt 自然终止 worker，无进程终止注入；status 收敛为 cancelled，result 可读，再次 run 成功写入 `HOST_AFTER_INTERRUPT_OK 中文`；退出后状态、任务输入、结果、图片及回执均已清理 |
| 桌面输入框与审批 | 在已有测试任务中输入 `claude status`、`/claude access allow .`，均显示 Hook 拦截结果；真实 Write 暂停，批准后同一任务完成，文件精确为 `UI_CHAIN_OK 中文` |
| 重启后的桌面运行、停止与审批 | 新版 Hook 恢复拦截，旧取消结果可读；桌面停止按钮终止实际 worker，预定文件未生成，随后 status 收敛为 cancelled；再次 run 正常进入 Write 审批，allow once 后同一任务完成并回读，文件精确为 `UI_RESTART_OK 中文`（20 字节，无 BOM 或末尾换行），activeJob 清空 |
| 原生 PNG 与 image/png 剪贴板 | 两种真实 MemoryStream 均捕获为 1577 字节 PNG；用 `Buffer.equals` 比较与原图逐字节一致，原始格式及 byteExact 标记正确 |
| 原生 Bitmap 剪贴板 | 真实位图转换为 PNG，300 × 180 尺寸及红、蓝、白三处采样像素匹配；按设计标记 byteExact 为 false |
| 原生多文件剪贴板 | 一次捕获两张中文文件名的 PNG，顺序、文件名和原始字节均正确 |
| 桌面图片入队与重复检测 | 实际输入 `claude image add` 加入两张图片；保持剪贴板不变再次执行时明确拒绝，队列记录与两份文件均保持不变 |
| 桌面双图片识别与清理 | 重新打开的测试任务在未授权时正确拒绝运行；授权后，同一队列经 `claude image run` 交给真实模型，分别识别两图的白底、左侧红色正方形与右侧蓝色圆形，并判断两图相同；结果回到桌面，任务成功，队列与 activeJob 清空，两份捕获文件均已删除 |
| 桌面插件管理 | 插件详情显示 0.3.6，MCP、Skill 和三个 Hook 正常展示；三个 Hook 均已启用 |
| `safe` 的实际隔离 | 独立探针没有可用插件 MCP、用户或插件 Skill、Hook 事件；Claude 初始化事件仍列出已安装插件名称，这份名称清单不等于插件功能启用 |

真实测试开始时，原模型服务返回 HTTP 402 欠费错误。按用户要求切换 DeepSeek 后，上述读写、审批、Skill 和会话测试通过。服务配置遵循 [DeepSeek 官方 Claude Code 说明](https://api-docs.deepseek.com/quick_start/agent_integrations/claude_code/)。个人配置与凭据保存在仓库外，原配置已备份，没有写入测试夹具或提交。

## 验证边界

- 桌面插件详情、Hook 设置页、普通命令和斜杠别名、真实审批已通过界面验证。停止按钮暴露的任务回收缺陷已经修复，并通过新启动的真实宿主及重启后的实际桌面验证。
- Windows 原生剪贴板的 PNG、image/png、Bitmap 和多文件列表均已实测，桌面双图片全流程也已通过。数量与容量上限使用临时夹具验证；没有穷举所有应用提供的自定义剪贴板对象。
- 本轮真实模型验证覆盖上述固定任务。全部权限模式的参数映射、拒绝、回答问题及取消路径均有模拟测试，未逐一验证各账号的 `auto` 分类策略、广泛 `bypass` 操作或所有第三方插件。
- Claude CLI 对 DeepSeek 模型名产生过会话标题相关的 `unrecognized_model` 诊断，任务仍返回成功且实际文件正确。CLI 报告的费用字段未与 DeepSeek 账单核对，不作为实际费用结论。
- 安装时出现过旧缓存备份拒绝访问，使用新的缓存版本后成功。本机 App 使用 Codex 0.153.4；更新插件后，原桌面任务的命令进入了宿主模型，刷新插件和 Hook 设置页未恢复拦截。官方 [0.154.0 更新记录](https://learn.chatgpt.com/docs/changelog)列出了外部插件升级后刷新会话 Hook 的修复，该记录与本次现象一致，但没有据此声称已验证其全部根因。用户重启 App 后，实际桌面的命令拦截、取消结果读取、停止、状态恢复、再次运行和审批均已通过。
- 独立 CLI 仍有共享配置未知键、其他 Skill 图标路径和 PowerShell snapshot 警告，未为隐藏这些宿主警告修改无关配置。
