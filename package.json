{
  "name": "codex-claude-code-bridge",
  "version": "0.3.1",
  "description": "A deterministic command and MCP bridge from Codex to local Claude Code with runtime permission approvals and visible conversation inheritance.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "check": "node ./scripts/check.mjs && node --test",
    "register": "node ./scripts/register-personal-marketplace.mjs",
    "unregister": "node ./scripts/unregister-personal-marketplace.mjs --yes"
  },
  "license": "MIT"
}
